let letter = "";
while (letter !== "X") {
  letter = prompt("Type a letter or X to exit:");
}

// Ultimately, choosing which loop to use depends on context.  All loops can be writte `while`, but if you know in advance how many times you want the loop to run, `for` is the best choice. 