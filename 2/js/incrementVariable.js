let b = 0; // b contains 0
b += 1; // b contains 1 .  NOTE: += is an increment operator that allows incrementation.
b++; // b contains 2 .  NOTE:  ++ is an increment operator that allows incrementation
console.log(b); // Shows 2

// Incrementation is to increase a variable's value by 1.