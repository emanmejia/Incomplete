const firstName = prompt("Enter your first name:");
const lastName = prompt("Enter your last name:");
alert(`Hello, ${firstName} ${lastName}`); // this JS command gave a second pop up that output the result, "Hello, ${name}"