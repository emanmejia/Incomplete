const number = Number(prompt("Enter a number:"));
if (number > 0) {
  console.log(`${number} is positive`);
}

// only positive numbers are shown, if the number is zero or negative it does not present anything in the console. 