const number = Number(prompt("Enter a number:"));
if (number >= 0) {
  console.log(`${number} is positive or zero.`)
}

// If the user input is 0, the mesage appears in the console, which means that the condition (number >= 0) was satisfied. 